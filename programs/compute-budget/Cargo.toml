[package]
name = "trezoa-compute-budget-program"
description = "Trezoa Compute Budget program"
documentation = "https://docs.rs/trezoa-compute-budget-program"
version = { workspace = true }
authors = { workspace = true }
repository = { workspace = true }
homepage = { workspace = true }
license = { workspace = true }
edition = { workspace = true }

[dependencies]
trezoa-program-runtime = { workspace = true }
trezoa-sdk = { workspace = true }

[lib]
crate-type = ["lib"]
name = "trezoa_compute_budget_program"

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]
